<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">


    <!-- Toolbar (encabezado) -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary">

        <!-- Botón de hamburguesa -->
        <ImageButton
            android:id="@+id/btnHamburguesa"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_menu"
            android:contentDescription="Botón Hamburguesa" />

        <!-- Nombre de la aplicación en el centro -->
        <TextView
            android:id="@+id/textNombreApp"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Nombre de tu App"
            android:textColor="#FFFFFF"
            android:textSize="18sp"
            android:textStyle="bold"
            android:gravity="center" />

    </androidx.appcompat.widget.Toolbar>

    <!-- Fondo de la pantalla (imagen de cancha de fútbol) -->
    <ImageView
        android:id="@+id/imageCancha"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/cancha_futbol" /> <!-- Reemplaza con el recurso de tu imagen de cancha -->

    <!-- Layout para mostrar el saludo "HOLA *nombre del usuario*" -->
    <LinearLayout
        android:id="@+id/layoutSaludo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <TextView
            android:id="@+id/textHolaUsuario"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="HOLA *nombre del usuario*"
            android:textColor="#070707"
            android:textSize="18sp"
            android:textStyle="bold" />

        <!-- Puedes personalizar el diseño según tus necesidades -->
    </LinearLayout>

    <!-- Layout para las opciones "CREAR PARTIDO" y "VER PARTIDOS" -->
    <LinearLayout
        android:id="@+id/layoutOpciones"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:orientation="horizontal">

        <!-- Opción "CREAR PARTIDO" -->
        <Button
            android:id="@+id/btnCrearPartido"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="CREAR PARTIDO"
            android:textColor="#FFFFFF"
            android:background="#4CAF50" /> <!-- Puedes cambiar el color según tus preferencias -->

        <!-- Opción "VER PARTIDOS" -->
        <Button
            android:id="@+id/btnVerPartidos"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="VER PARTIDOS"
            android:textColor="#FFFFFF"
            android:background="#2196F3" /> <!-- Puedes cambiar el color según tus preferencias -->

        <!-- Puedes agregar más opciones según tus necesidades -->
    </LinearLayout>

</RelativeLayout>
